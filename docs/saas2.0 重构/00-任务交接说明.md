# SaaS 2.0 多银行渠道 Front 重构任务交接说明

> Wiki 入口：[WIKI-START.md](./WIKI-START.md)
> 状态：current（持续设计中）
> 记录日期：2026-08-04
> 任务类型：架构与接口设计
> 当前阶段：公共框架、交易字段契约及中信三项交易查询契约已创建；渠道流水已完成按“银行 + 交易业务”拆表的 DDL 基线，等待逐银行 Handle 和持久化实现
> 目标读者：后续继续本任务的 AI、架构人员和业务开发人员

---

## 1. 任务目标

重新设计一套面向内部供应链等业务系统的多银行渠道支付 Front。

新 Front 的目标不是兼容旧系统对象，而是：

1. 向内部业务系统提供统一的交易、账户和查询能力；
2. 通过 Router 按银行编码选择银行 Handle；
3. 在银行 Handle 中完成钱包协议组装、签名加密、调用和响应转换；
4. 由 Front 管理租户银行配置和渠道交易流水；
5. 新增银行时不修改公共租户配置对象，不影响既有银行实现；
6. 对外隐藏 `bizFunc`、`channelNo`、银行 URL、密钥和算法等协议细节。

首期银行：

- 中信银行；
- 平安银行。

旧 `fund-catering-front-service` 只作为代码结构和银行接口实现参考，不作为新项目兼容基线。

---

## 2. 开始继续设计前必须阅读

本目录文档：

1. [00-任务交接说明.md](./00-任务交接说明.md)
2. [01-front-重构总体结构设计.md](./01-front-重构总体结构设计.md)
3. [02-中信银行接口能力汇总.md](./02-中信银行接口能力汇总.md)
4. [03-平安银行接口能力汇总.md](./03-平安银行接口能力汇总.md)
5. [04-front-service完整重构实施方案.md](./04-front-service完整重构实施方案.md)
6. [05-front代码开发约束.md](./05-front代码开发约束.md)
7. [06-transfer-consume字段契约.md](./06-transfer-consume字段契约.md)
8. [07-transferAuth-resendTransferAuthCode字段契约.md](./07-transferAuth-resendTransferAuthCode字段契约.md)
9. [08-withdraw-refund-platform-transfer字段契约.md](./08-withdraw-refund-platform-transfer字段契约.md)
10. [09-channel-transaction-ddl.md](./09-channel-transaction-ddl.md)
11. [09A-channel-transaction-table-field-catalog.md](./09A-channel-transaction-table-field-catalog.md)
12. [10-transaction-query-field-contract.md](./10-transaction-query-field-contract.md)

其中：

- `01` 记录总体设计及讨论过程；
- `02/03` 保留两家银行接口能力依据；
- `04` 是公共框架、业务契约、LiteFlow、Router、渠道流水和银行 Handle 骨架的实施基线；
- `05` 是后续代码开发的硬约束，新增或修改 Front 代码前必须阅读；
- `06` 是实现中信、平安 transfer/consume 的字段和响应映射基线。
- `07` 是实现平安 transferAuth/授权码发送重发的字段、加密和响应映射基线。
- `08` 是实现两家 withdraw/refund 及中信 platformPay/platformReceive 的字段和响应映射基线。
- `09` 是所有交易落库、重复交易检查、状态查询和退款原交易关联必须遵守的分银行、分交易业务表基线。
- `09A` 将 10 张表的全部字段、类型、NULL、默认值、更新规则、注释和索引逐表展开，用于生成或审查最终 SQL。
- `10` 固定交易状态、平台交易明细和账户/登记簿交易明细的对象结构，并确认中信 `74/25/24` 契约。

银行源文档：

```text
/Users/limeng/workspaces/IdeaProjects_saas_dep/cateringsass/docs/
中信E管家产品客户钱包应用平台_接口文档-内部集成平台v4.7.doc

/Users/limeng/workspaces/IdeaProjects_saas_dep/cateringsass/docs/
客户钱包应用平台_接口文档-平安项目(总)v5.5.doc
```

旧代码参考：

```text
/Users/limeng/workspaces/IdeaProjects_lsym_dep/slhy/
fund-catering/fund-catering-front/fund-catering-front-service
```

中信真退款最新实现参考：

```text
/Users/limeng/workspaces/IdeaProjects_lsym_uat/slhy
分支：lsym_20260625_limeng_refundTask
提交：3dff8255d6（1.front 退款接口对接）
关键类：ZxTransConsumeCancelHandle、ZxRefundRequest、SaasZxInterService
```

该分支已经把中信退款从 `ZxTransferRequest + zxTransfer + bizFunc=27` 改为
`ZxRefundRequest + zxRefund + bizFunc=23`，是当前中信真退款字段映射的最新代码参考。
但它仍由请求直接传入 `orgPay/orgRec/orgTrans*`，并存在记录完整请求、响应、账户标识和 appKey 的日志，
新 Front 只能参考银行字段，不能复制其数据来源和日志方式。

中信平台收付款参考：

```text
/Users/limeng/workspaces/IdeaProjects_mdl_dep/mdl/
fund-catering-front/fund-catering-front-service
```

---

## 3. 已确认的核心约束

后续设计不得在没有明确讨论的情况下推翻以下约束。

### 3.1 新项目边界

- 这是重新设计的新多银行 Front；
- 老系统只参考 Router、Handle 和银行请求组装方式；
- 不复用老系统的 `operator/orgCode/firstCode` 配置定位方式；
- 不要求兼容旧请求、旧响应和旧数据库结构。

### 3.2 租户和银行

- `platformCode` 表示银行编码；
- 一个租户只能启用一家银行渠道；
- 租户银行配置使用：

```text
tenantId + platformCode
```

- Java 字段使用 `tenantId`；
- JSON、数据库字段可使用 `tenant_id`；
- Front 应校验请求中的 `platformCode` 与租户启用的银行一致。

### 3.3 银行配置

以下配置由 Front 根据 `tenantId + platformCode` 从新配置系统获取：

```text
appId/appKey/url
mchntId/mchntMbrId
银行账户特定静态配置
```

配置系统边界使用 `JSONObject`，经通用组装父类和银行策略转换为：

```text
TenantBankAccountConfig
├─ 跨银行通用强类型字段
└─ accountSpecialData: JSONObject
```

不得创建一个不断增加中信、平安及未来银行专属字段的公共账户配置大对象。
对银行内部通用、但不跨银行的字段，必须保留在对应银行的 `accountSpecialData`。

### 3.4 银行协议参数

以下参数只能由银行 Handle 决定：

```text
bizFunc
channelNo
具体钱包接口路径
签名和加密执行方式
钱包 reserveMap 映射规则
银行响应转换规则
```

业务系统不能通过 `specialData` 指定或覆盖这些参数。

---

## 4. 已确定的请求结构

### 4.1 对外请求为两部分

```java
public class FrontRequest<T extends FrontBaseRequestData> {
    // 租户、门店、银行及当前业务的银行无关强类型基础数据
    private T baseData;

    // 当前银行、当前接口的特殊数据
    private JSONObject specialData;
}
```

所有强类型基础数据继承公共定位信息：

```java
public class FrontBaseRequestData {
    private String tenantId;
    private String storeId;
    private BankCode platformCode;
}
```

两部分分别是：

1. `baseData`：租户、门店、银行定位信息和交易/交易查询/账户查询的公共强类型字段；
2. `specialData`：`JSONObject` 格式的银行及接口特殊字段。

已确认的公共字段补充约束：

- 交易基础对象包含 `payStoreNo/payStoreId/recStoreNo/recStoreId` 两组收付款门店字段；
- 单笔交易状态查询对象包含 `frontSsn/bizOrderNo/bizSubOrderNo`，其中后两者是业务主流水和业务子流水；
- 交易、交易查询和账户查询请求都通过外层 `FrontRequest.specialData` 提供当前能力的银行特殊字段。

### 4.2 Front 内部上下文

路由和能力校验通过后，`AbstractBankHandle` 根据 `tenantId + bankCode` 加载银行配置并形成：

```java
public record BankRequestContext<T extends FrontBaseRequestData>(
    T baseData,
    JSONObject specialData,
    TenantBankConfigSnapshot tenantBankConfig) {
}
```

`tenantBankConfig` 不由业务系统传入；外部 `FrontRequest` 仍然只有 `baseData + specialData` 两段。
Application Service 已通过 `FrontFlowContext.from(request, capability)` 把请求转换为统一业务 Slot，
路由后再由 `AbstractBankHandle.prepareContext()` 形成 `BankRequestContext` 并将租户银行配置回填到 Slot。
当前已落地 Context、执行阶段、LiteFlow `FlowExecutor`、NodeComponent 和 13 条 EL 规则链；
Application Service 统一通过 `FrontFlowExecutor` 进入规则链，不再直调 Handle。
`TenantBankConfigSnapshot.accountConfig` 使用 `TenantBankAccountConfig` 承载银行账户配置：

```text
TenantBankAccountConfig
├─ appId/appKey/url/mchntId/mchntMbrId
└─ accountSpecialData: JSONObject
```

`accountSpecialData` 是租户银行账户的特定静态配置，与当前交易/查询请求的
`specialData` 是两个独立 `JSONObject`，不得合并或相互覆盖。平安账户特定字段为
`txnClientNo/mrchCode/stlAcctNo`，其中 `stlAcctNo` 是资金汇总账号；中信账户特定字段为 `default_role/default_fund_type/self_role/`
`self_fund_type/self_dealType/self_store_no/self_store_id`。中信这 7 个字段对中信各交易能力
是通用账户配置，但不上提到跨银行通用属性。

### 4.3 `specialData` 的边界

`specialData` 使用 `JSONObject`，不使用 `Map<String, String>` 包装多层 JSON 字符串。

可以包含：

- 当前银行和接口特有的可变业务字段；
- 暂未进入公共强类型对象的条件字段；
- 银行特有备注、分润扩展或查询扩展。

不能包含：

- 银行商户和密钥配置；
- 银行账户 `accountSpecialData`；
- `bizFunc`、`channelNo`；
- 银行 URL；
- 签名、加密算法；
- Front 流水生成规则；
- 已经属于公共核心业务语义的金额、订单、账户、查询场景等字段。

每个“银行 + 业务接口 + schemaVersion”必须定义：

- 允许字段白名单；
- 必填和条件必填规则；
- 字段类型、长度、枚举；
- 敏感字段处理方式；
- 到银行 `reserveMap` 的显式映射。

禁止直接把调用方 `specialData` 全量 `putAll` 到银行请求。

---

## 5. 已确定的路由与 Handle 结构

### 5.1 Router 只负责选银行

首期按业务大类建立两个 Router：

```text
TransactionRouter
QueryRouter
```

Router Key：

```text
platformCode
```

不把以下内容放入 Router Key：

- `QueryCapability`；
- `bizFunc`；
- `channelNo`；
- 账户类型；
- 银行交易类型。

### 5.2 查询使用银行“大 Query Handle”

已确认的查询结构：

```text
QueryRouter(platformCode)
  → CiticQueryHandle
  → PingAnQueryHandle
```

具体查询能力由 Handle 方法确定，例如：

```java
queryTransactionStatus(...)
queryPlatformTransactions(...)
querySubAccountTransactions(...)
queryAccountBalance(...)
queryAccountStatus(...)
```

如果对外 API 拆成上述具体方法，则请求不需要额外传 `QueryCapability`。

如果只暴露一个通用 `/query`，则必须在核心业务数据中传 `QueryCapability`，但它仍然不进入 Route Key。

### 5.3 LiteFlow 定位

LiteFlow 用于公共流程编排：

```text
请求校验
→ Router 与能力校验
→ AbstractBankHandle 装配租户银行配置上下文
→ 重复交易校验
→ 渠道流水预创建
→ Handle 业务方法调用
→ 响应归一化
→ 渠道流水更新
→ 异常处理
```

业务系统可以使用 LiteFlow `SWITCH` 选择对应银行的 `specialData` 组装工具。

Front 内部银行选择仍使用 Router/Handle，不让 LiteFlow 规则直接承载银行协议路由。

### 5.4 新旧 Handle 映射结论

当前 13 个业务 Handle 方法与旧 `fund-catering-front` 的关系已经固定：

- 普通转账、授权转账、消费、退款、提现、平台付款、平台收款均保留旧业务语义；
- 授权码重发对应旧 `sendSmsVerification`；平安没有独立重发协议，当前统一为生成新银行流水后再次
  调用 `bizFunc=26`，不传原短信指令号；
- 账户状态对应旧 `AccountHandle.acctState`，该方法只存在于 lsym 参考版本；
- 账户余额对应旧 `BasTransQueryHandle.queryAccInfo`；
- 新 `queryTransactionStatus` 只映射单笔状态 `queryTransStatus`；中信 `bizFunc=73` 是文件处理状态，
  不得合并进该方法；
- 平台明细固定对应中信 `bizFunc=25/chnlNo=0010`；账户/登记簿明细固定对应中信
  `bizFunc=24/chnlNo=0010`，两者不再按交易类型拆更多 Handle 方法；
- `queryWithDrawFee` 不再保留独立能力，由平台明细/账户交易查询按交易类型覆盖；
- 回执验证码只在平安旧实现中存在，中信对应方法返回 `null`。它实际是“查询明细单验证码”，若后续纳入应按平安特有能力单独设计；
- 转账召回、回执验证码、开户绑卡和文件处理不在首期范围。

完整逐方法映射见 `04-front-service完整重构实施方案.md` 的“11.6 当前 Handle 方法与旧 Front Handle 映射”。

---

## 6. 当前业务范围

### 6.1 首期交易

当前需要设计：

1. 消费；
2. 普通转账；
3. 短信鉴权转账；
4. 转账或提现验证码申请；
5. 提现；
6. 退款；
7. 平台付款；
8. 平台收款。

“授信转账”名称尚未确认。现有材料只证明平安存在“短信鉴权转账”，没有证明存在使用授信额度的转账。

暂未纳入首期，但需要在能力矩阵中保留：

- 交易撤销；
- 转账召回；
- 提现退票处理；
- 充值；
- 实时预付；
- 冻结、解冻；
- 分账；
- 文件和批量交易。

### 6.2 首期账户查询

当前需要设计：

- 平台资金账户余额；
- 用户/子账户余额；
- 功能子账户余额；
- 用户或账户状态。

### 6.3 首期交易查询

当前需要设计：

- 转账、消费、提现等交易状态查询；
- 平台交易明细查询；
- 子账户交易明细查询。

文件查询暂不属于首期。

---

## 7. 两家银行的关键业务映射

### 7.1 交易

| Front 业务 | 中信 | 平安 |
|---|---|---|
| 消费 | `/transfer`，`bizFunc=27` | `/transfer`，`bizFunc=01`，`functionFlag=6/7/9` |
| 普通转账 | `/transfer`，`bizFunc=27` | `/transfer`，`bizFunc=01`，`functionFlag` 由场景策略选择 `6/7/9` |
| 短信鉴权转账 | 未确认支持 | 验证码申请 `26` + 转账 `45` |
| 提现 | `/withdrawal`，当前 `bizFunc=26` | `/withdrawal`，当前 `bizFunc=01`；`36` 暂不激活 |
| 退款 | 真退款 `/refund`，`bizFunc=23` | 真退款 `/refund`，当前 `bizFunc=02`；`06` 暂不激活 |
| 平台付款 | `/transfer`，`bizFunc=2041` | 明确 `UNSUPPORTED` |
| 平台收款 | `/transfer`，`bizFunc=2042` | 明确 `UNSUPPORTED` |

### 7.2 账户

| Front 能力 | 中信 | 平安 |
|---|---|---|
| 平台资金余额 | `35/36` | `63` |
| 用户/子账户余额 | `46` | `64` 或列表 `01/02/03` |
| 用户状态 | `2058` | 未找到完全等价能力 |

### 7.3 交易状态

| Front 能力 | 中信 | 平安 |
|---|---|---|
| 普通交易状态 | `bizFunc=74` | `bizFunc=02` |
| 提现状态 | `bizFunc=74` | `bizFunc=03` |

中信用一个较通用的 `74` 查询普通交易状态；平安在同一状态查询 API 下按原交易类型选择不同 `bizFunc`。

### 7.4 平台交易明细

中信：

```text
bizFunc=25
01 转账入金
02 退汇
03 支付渠道入金
04 提现
05 退款（预留）
99 所有
```

单个查询日期和交易类型由业务系统放入请求 `specialData`；Handle 将 `transactionDate` 映射为银行
`TRANS_DATE`，并自行维护银行 `PAGE`。中信 24/25 不支持跨日，`bizFunc/chnlNo/TRANS_DATE/PAGE`
不允许业务系统覆盖。

平安没有完全等价的一个接口：

```text
04 普通转账充值明细
05 会员时间段交易明细
08 清分、提现和部分退款明细
02 提现退票
39 银行费用扣收结果
```

不能假设平安可以用一个 `bizFunc` 返回中信 `25` 的全部分类。

### 7.5 子账户交易明细

中信 `bizFunc=24`：

```text
01 入金分账
02 交易划转
03 提现
04 提现手续费
05 提现退汇
06 渠道来账
98 所有明细
99 所有汇总
```

查询日期范围、交易类型和登记簿/账户类型由业务系统放入请求 `specialData`。账户类型当前按中信 v4.7：

```text
01 公共调账登记簿
12 平台自有资金登记簿
13 担保登记簿
17 待结算手续费登记簿
```

平安最接近的是 `bizFunc=05`：

```text
queryFlag=1 全部
queryFlag=2 转出
queryFlag=3 转入
```

平安提现、清分等明细还需要 `bizFunc=08`。

---

## 8. 渠道交易流水设计约束

Front 渠道流水必须同时按银行和交易业务拆分，不能使用一张统一渠道表：

```text
中信：transfer / consume / refund / withdraw / platform_pay / platform_receive
平安：transfer / consume / refund / withdraw
```

平安 `transferAuth/resendTransferAuthCode` 与普通转账进入平安转账表，由 `capability` 区分；中信不创建
授权空表，平安不创建平台收付款空表。精确表名、字段和索引见 `09-channel-transaction-ddl.md`。

用途：

1. 保存 Front 实际请求银行的数据和最终结果；
2. 关联业务主订单、子订单；
3. 按 `tenantId + bizOrderNo + bizSubOrderNo` 防止重复交易；
4. 退款关联原交易；
5. 状态查询时确定原银行交易类型；
6. 平安退款时确定使用 `bizFunc=02/06`；
7. 平安状态查询时确定使用 `bizFunc=02/03`；
8. 支持超时、未知状态和后续补偿。

每张表必须保存的字段组：

```text
tenant_id
store_id
capability
front_ssn / front_query_id
biz_system_code / biz_transaction_type
biz_transaction_id / biz_sub_transaction_id
biz_request_no / biz_order_no / biz_sub_order_no
pay_store_no / pay_store_id / rec_store_no / rec_store_id
amount / fee / currency
pay/rec/withdraw/bank_card 相关明确字段
bank_biz_func
bank_channel_no
bank_query_id
bank_user_ssn
wallet_resp_code / wallet_resp_desc
bank_resp_code / bank_resp_desc / bank_status
front_status
reserve1 / reserve2 / reserve3
create_time / update_time / bank_responded_at
```

退款表另存原能力、原渠道记录、原 Front 流水及原业务主/子记录；转账、消费表另存累计确认退款金额。
业务系统不得传入物理表名，表路由由 Front 根据 `platformCode + capability` 显式选择固定 Repository。
渠道表不保存整段请求或响应快照。账户、会员、姓名、卡号等内部业务字段允许按明确列保存原始值，
本期不要求数据库字段加密；日志、异常消息和普通接口响应仍不得输出。

### 8.1 交易状态建议

```text
INIT
SENDING
ACCEPTED
PROCESSING
SUCCESS
FAILED
UNKNOWN
RETURNED
REFUNDED
```

特别注意：

- HTTP 成功不是交易成功；
- 钱包平台受理成功不是银行最终成功；
- 平安提现同步成功只表示受理；
- 超时、无响应不能直接记为失败，应进入 `UNKNOWN` 并主动查询。

---

## 9. 已确定的统一交易响应

单条接口使用公共 `R` 包装确定类型结果，不再增加 `FrontResponse` 中间层：

```java
R<FrontTransactionResult>

public class FrontBaseResult {
    private String frontRespCode;
    private String frontRespDesc;
    private JSONObject specialData;
}
```

`R.code/msg` 表示工程统一调用结果；`R.data` 是跨银行公共强类型结果；
`R.data.specialData` 表示银行特殊结果。Front 公共错误码统一放入
`catering-common-core`，功能模块和 API 模块不得重复定义。

分页明细查询直接返回工程统一的 `TableDataInfo<TransactionDetailItem>`，禁止使用 `R` 二次包装。
每条 `TransactionDetailItem.specialData` 保存该笔明细对应的银行 `reserveMap` 白名单映射结果。

`specialData` 不得包含密钥等配置数据；日志默认不得打印完整 `specialData`。

交易类统一基础字段：

```text
front_resp_code
front_resp_desc
front_ssn
front_status
front_query_id
front_remark
front_trans_dt
front_trans_tm
```

Java 对象建议使用 camelCase：

```text
frontRespCode
frontRespDesc
frontSsn
frontStatus
frontQueryId
frontRemark
frontTransDate
frontTransTime
```

JSON 是否输出 snake_case 由统一序列化规范决定，不要在不同接口中混用。

查询类后续需要设计各自的：

- 查询基础响应；
- 分页响应；
- 账户余额结果；
- 账户状态结果；
- 交易状态结果；
- 平台交易明细项；
- 子账户交易明细项。

不要让所有查询共用一个包含大量可空字段的大结果对象。

---

## 10. 已发现的源文档和旧代码问题

### 10.1 平安

1. 文件名为 5.5，封面仍显示 5.4。
2. `functionFlag=7` 免密支付已增加，但退款说明只明确 `6/9`。
3. 账户查询 `01/63/64` 与部分响应说明中的 `03` 存在交叉。
4. 账户列表出现 `01/02/03`，部分说明只覆盖 `01/02`。
5. 交易明细目录列出 `43`，公共请求说明主要列到 `40`。
6. 平安同步提现成功不是终态。
7. 平安多个查询用途的返回数组结构不同。
8. 旧代码有注释与实际 `bizFunc` 不一致的情况。
9. 旧代码部分查询方法仍直接返回 `null`。

### 10.2 中信

1. 中信短信验证码接口只用于用户签约，不是转账鉴权。
2. 版本历史已经移除 `SIGN_INFO`，旧代码仍可能保留。
3. 交易请求金额通常为分，部分明细响应金额为元。
4. 旧代码的中信授权转账和验证码实现均是模拟成功，新 Front 两项都必须标记 `UNSUPPORTED`。
5. 当前退款代码只覆盖了较简单的退款场景。
6. 状态查询存在受理、成功、失败、处理中、退款、退汇等多种状态。
7. 旧代码部分字段的 `queryId/transSsn/userSsn` 映射需要重新核对。

旧代码只能证明“曾经如何实现”，不能替代银行文档和正式联调确认。

---

## 11. 需要业务或产品优先确认

### P0：阻塞接口模型

1. “授信转账”是短信鉴权转账，还是实际授信额度转账。
2. 平安消费和普通转账分别使用 `functionFlag=6/7/9` 中的哪一种。
3. 平安后续是否增加 `bizFunc=36` 短信提现；当前首版只使用真实 Handle 的 `01`。
4. 平安免密支付 `functionFlag=7` 是否支持退款。
5. 退款首期是否支持部分退款、手续费退款、分润退款、平台出资退款。
6. 平安无法等价支持中信查询类型时，是返回不支持，还是由 Front 多次查询后聚合。
7. 是否把撤销、转账召回、提现退票纳入首期。

中信明细查询不再抽象含义不清的统一“实收”或“所有”：业务系统通过中信 specialData 常量明确选择
`01 入金分账/06 渠道来账`，并明确区分 `98 所有明细/99 所有汇总`，因此这两项不再阻塞接口模型。

### P1：字段和运行规则

1. 金额统一使用分还是 `BigDecimal` 元；
2. 日期和时间的统一 Java 类型；
3. 主订单、子订单、原订单的唯一性；
4. Front 重复交易键生成方式；
5. 查询日期范围、分页、排序规则；
6. 状态查询轮询间隔、最大次数和人工处理条件；
7. 银行特有字段的 `specialData schemaVersion`；
8. 渠道明确字段的留存期限；完整请求、`specialData`、银行请求和银行响应快照明确不保存；
9. 通用查询结果最终字段；
10. 新配置系统的接口形式和缓存策略。

---

## 12. 建议的后续推进顺序

不要一次设计全部对象。建议一次只处理一个业务，并同步更新本目录文档。

### 第一阶段：查询

1. 交易状态查询；
2. 平台交易明细查询；
3. 子账户交易明细查询；
4. 平台和子账户余额查询；
5. 账户状态查询。

优先查询的原因：

- 查询差异最大；
- 可以先确定通用响应对象；
- 后续交易补偿、状态轮询和渠道流水都依赖查询能力。

### 第二阶段：基础交易

1. 普通转账；
2. 消费；
3. 提现；
4. 退款。

### 第三阶段：银行差异交易

1. 平安短信鉴权转账；
2. 验证码申请；
3. 中信平台付款；
4. 中信平台收款；
5. 平安平台收付款能力确认后的实现。

### 每个业务的设计产出

每个业务至少形成：

1. Front 对外请求对象；
2. Front 对外响应对象；
3. 通用枚举；
4. 中信字段映射；
5. 平安字段映射；
6. 两家银行的 `specialData` schema；
7. 银行配置字段；
8. 钱包响应归一化；
9. 渠道流水写入和状态迁移；
10. 不支持能力和异常码；
11. 重复交易检查、超时、重试和状态查询规则；
12. 文档与代码验证依据。

---

## 13. 后续 AI 工作要求

继续本任务时应遵守：

1. 先按 `WIKI-START.md` 的必读顺序阅读文档，不重新从零推导总体结构。
2. 对银行能力结论必须同时核对源 Word 和旧代码。
3. 明确区分：

```text
文档明确支持
旧代码已经实现
旧代码测试桩或返回 null
仍需银行确认
```

4. 不把旧代码硬编码直接复制到新设计。
5. 不把 `bizFunc/channelNo` 重新放回业务系统请求。
6. 不把所有银行特殊字段塞进公共配置 Java 类。
7. 不为了表面统一而改变银行查询的真实语义。
8. 对不支持能力使用明确的 `CAPABILITY_NOT_SUPPORTED`，不能返回 `null` 或模拟成功。
9. 每完成一个业务的字段核对，就在本目录新增或更新对应 Markdown。
10. 新结论与现有约束冲突时，必须记录冲突原因并由用户确认。

---

## 14. 当前代码实施状态

当前代码按公共契约与运行时实现分为：

```text
catering-api/catering-api-front
└── API、请求响应模型、常量与枚举

catering-common/catering-common-core
└── 统一返回主体 R、Front 公共错误码与 FrontException

catering-modules/catering-front
└── 功能实现、异常转换与服务配置
```

原 `catering-front-api`、`catering-front-common` 已合并到工程既有的
`catering-api-front`；`catering-front` 本身是 Jar 服务模块并直接依赖该 API 制品及
`catering-common-core`，不再嵌套
`catering-front-service` 子模块。

已实现的骨架：

- 13 个首期 API：单条接口返回 `R<具体结果>`，分页明细查询直接返回 `TableDataInfo<具体行>`；
  `FrontBaseResult` 统一提供 `frontRespCode/frontRespDesc/specialData`；
- `TransactionRouter`、`QueryRouter`；
- 构造器注入 `List<Handle>` 的不可变 Registry；
- 中信、平安 Transaction/Query 四个 Handle Bean；
- `TenantBankConfigProvider`、`TenantBankConfigSnapshot`、`TenantBankAccountConfig` 配置边界；
- 银行账户配置的通用组装父类、平安/中信策略和重复注册校验路由；
- `constant.front` 下集中定义配置查询原始 key 及通用、平安、中信账户配置 `JSONObject` 字段 key；
- `constant.front` 下新增 `FrontBankRequestConstants`、`FrontBankResponseConstants`、`CiticTransferContractKeys`、
  `PingAnTransferContractKeys`，固化 transfer/consume 字段、单位、固定协议值和两家银行不同成功码；
- `constant.front` 下新增 `PingAnTransferAuthContractKeys/PingAnTransferAuthCodeContractKeys`，只保留
  平安 v5.5 与 mdl 真实调用使用的短信鉴权转账、授权码申请字段；
- `constant.front` 下新增两家退款、提现字段常量和中信平台收付款字段常量；中信退款明确使用
  `/refund + bizFunc=23`，平安平台付款/收款明确为 `UNSUPPORTED`；
- 中信退款常量已经与 lsym UAT `lsym_20260625_limeng_refundTask` 真退款实现核对；活动 reserve 字段一致，
  `FUND_TP` 在新 Front 中不得照搬其 `platformUserRole` 来源；
- `CiticTransferContractKeys` 已按 mdl 当前 transfer/consume 实际映射收缩；未使用的
  `USER_SHARE_*`、`REQ_RESERVED` 不作为活动常量，`P_SELF_FLAG/P_SELF_AMT` 当前固定为 `N/0`；
- `BaseTransactionBusinessData` 统一提供单位为分的 `amount/fee` 及内部业务系统公共字段；
  银行使用的账户、会员编号、姓名和卡号统一进入请求 `specialData`；
- `AuthTransferBusinessData` 只继承公共转账字段，平安短信字段固定进入请求 `specialData`；
  `TransferAuthCodeBusinessData` 补齐付款账户、付款会员、收款账户，删除银行不使用的手机号和旧授权号；
- 授权码发送/重发使用明确返回 `FrontTransferAuthCodeResult`，公共返回 `frontSsn/frontQueryId`，
  平安 `smsIdx/receiveMobile` 解密后进入响应 `specialData`；
- `FrontBaseResult.applyFrontResponse(FrontErrorCode)` 同时设置统一响应码与响应说明；
- Front 业务成功统一使用全局 `R.SUCCESS=200`；银行业务失败、钱包业务失败以及其他 Front 业务失败
  必须通过 `R.fail(message, result)` 返回顶层失败码，并在 data 内保留统一码、说明和状态；
- `AbstractBankHandle` 按 `tenantId + bankCode` 统一查询、校验配置并装配三段式
  `BankRequestContext`；
- `FrontFlowContext.from()` 完成 `FrontRequest → 统一业务 Slot` 转换，`FrontExecutionInfo` 记录
  能力、配置版本、执行阶段和关键时间；
- `PENDING_INTEGRATION`、`UNSUPPORTED` 能力状态；
- Handle 注册、重复注册、业务分派、路由结果、能力状态、处理结果、耗时和异常日志；
- 早期骨架曾完成路由、重复注册、能力状态、二段式序列化及启动冒烟验证；
- 按当前用户约束，代码仓库不保留测试类，后续只有收到明确要求时才新增测试和执行编译验证；
- 已补充当前 17 个 Handle 契约方法与旧 Front 方法的逐项映射，其中包括 4 个公共方法、8 个交易方法和 5 个查询方法；新增公共方法为 `prepareContext(request)`，旧 Front 无直接对应；13 个交易/查询方法已进一步关联到 mdl 的 API、Service、中信/平安具体实现类及真实实现状态。

日志只记录租户、门店、银行、能力及 Handle 等定位字段，禁止直接记录完整 `specialData`、
`accountSpecialData`、账户配置和敏感字段。

LiteFlow、真实远程租户银行配置 Provider，以及渠道 Entity/VO/Mapper/Service/Handle 写入骨架已接入；
渠道流水 10 张表尚未在目标环境执行建表。交易发送前执行的是重复交易检查，不是旧结果重放幂等：
当前银行业务表内 `tenantId + bizOrderNo + bizSubOrderNo` 相同即返回“交易已存在”。
当前已更新 transfer/consume、平安 transferAuth/授权码发送重发、两家 withdraw/refund 和中信
platformPay/platformReceive 契约及代码框架，遵照用户要求
未运行编译或测试。

### 14.1 本轮新增实现（2026-08-04，银行 Handle + LiteFlow 编排；查询状态已按 2026-08-05 修正）

本轮按用户授权完成银行 Handle 和 LiteFlow 编排接入。后续复核已将平安全部查询能力统一改为
`PENDING_INTEGRATION`；以下历史编译记录只说明当时工程状态，不代表这些查询字段已确认。

1. **LiteFlow 编排接入（按 04 号 §14 原设计 + 异常模式按 05 号 §7.3 新增）**：
   - 7 个通用节点：请求校验、路由能力校验、上下文装配、重复交易检查、
     交易/查询分派和响应归一化；渠道流水写入已下沉到 Handle；
   - 13 条链（`resources/liteflow/front-flow.xml`）：8 个交易方法 + 5 个查询方法，
     每业务一条 THEN 串行链；
   - `FrontFlowExecutor` 返回 Slot 结果，Application Service 根据 Slot/Front 响应码返回 `R.ok` 或 `R.fail`；
   - **业务异常不 throw**：节点内 `markBusinessFail` + `setIsEnd(true)`，系统异常 throw 走
     `FrontExceptionHandler`（见 05 号 §7.3）；
   - `BankHandle.requireCapability()` 改为返回 `IntegrationStatus`，新增 `checkCapability` 供非 LiteFlow
     路径直接抛异常；
   - `FrontFlowContext` 新增 `selectedHandle/frontRespCode/frontRespDesc/businessFailed` 字段
     及 `markBusinessFail` 方法；
   - 路由仍由 Registry（`TransactionHandleRegistry/QueryHandleRegistry`）承担，链内
     `FrontRouteAndCapabilityCheckCmp` 节点按 platformCode 查 Handle；新增银行只加 Handle Bean。
   - 不再保留独立流水创建/完成节点；重复交易检查和渠道状态写入由 Handle 完成。

2. **租户银行账户配置组装链真实接入**：`RemoteTenantBankConfigProvider` 通过
   `RemoteConfigServiceClient` Feign 调 catering-system 配置中心，按 `zx_bank_config`/`pa_bank_config`
   取原始配置，经已有 `BankAccountConfigAssemblerRouter` → `AbstractBankAccountConfigAssembler`
   （通用 5 字段）→ 中信/平安策略（accountSpecialData）组装为 `TenantBankAccountConfig`。

3. **中信银行基础设施**（`channel/citic/`）：
   - `CiticCryptoProperties`（SM2 密钥从 yml `citic.crypto.*` 读，不硬编码）；
   - `CiticSm2Crypto`（hutool SmUtil，C1C3C2）、`CiticOpenBodySigSigner`（OPEN-BODY-SIG HMAC-SHA256）；
   - `CiticWalletHttpClient`（hutool HttpRequest，连接/读超时、try-with-resources）；
   - `CiticBankResponseChecker`（5 位成功码 00000 判定）；
   - `CiticSequenceGenerator`（transSsn 40 位、laasSsn，ThreadLocalRandom 防碰撞）；
   - 8 个协议 DTO（CiticWalletRequest 基类 + transfer/withdraw/refund/query*）。

4. **中信 Handle 真实实现**（6 交易 + 5 查询）：
   - `CiticTransactionHandle`：transfer/consume（bizFunc=27）、withdraw（26）、refund（23，
     原业务主/子流水组与原中信流水二选一定位，原渠道字段待持久层补齐，FUND_TP 禁取 role）、
     platformPay/platformReceive（2041/2042，平台侧由商户自有资金登记簿隐式确定，不传平台账号）；
   - `CiticQueryHandle`：账户状态（2058）、账户余额（按 accountScope 35/36/46）、交易状态（74，
     status 映射 00~05）、平台明细（25，≤20/页）、登记簿明细（24，≤50/页，金额元转分）；
   - 中信 24/25 只支持单个 `TRANS_DATE`，不支持跨日；`beginDate/endDate` 文档明确忽略。

5. **平安银行基础设施**（`channel/pingan/`）：与中信对称，`PingAnBankResponseChecker` 判 6 位成功码
   000000，`PingAnSequenceGenerator` 生成 22 位 transSsn；签名器复用中信 `CiticOpenBodySigSigner`
   （算法相同）；`PingAnSm2Crypto/PingAnCryptoProperties` 独立。

6. **平安 Handle 实现状态**（6 个交易能力按现有契约；全部查询先人工核对）：
   - `PingAnTransactionHandle`：transfer/consume（01）、transferAuth（45，messageCheckCode SM2 加密）、
     resendTransferAuthCode（26，smsIdx/receiveMobile 解密后写入 specialData 禁日志）、
     withdraw（01，受理态 ACCEPTED）、refund（02，异步入账 ACCEPTED）；
     platformPay/platformReceive 固定 UNSUPPORTED；
   - `PingAnQueryHandle` 现有方法仅保留为分析草稿；账户状态、账户余额、交易状态、平台明细、交易明细
     全部标记 `PENDING_INTEGRATION`，等待人工逐接口核对后再启用。

7. **Service/Controller 返回约束**：每业务方法构造 Slot 调 `FrontFlowExecutor`；单条接口返回 `R<T>`，
   分页明细查询直接返回 `TableDataInfo<T>`，Controller 原样透传，不再使用 `R` 包裹分页结果。

8. **配置**：`catering-front/pom.xml` 加 mybatis/tenant/encrypt/redis/idempotent/api-system/security/
   liteflow 依赖；新增 `script/config/nacos/catering-front.yml`（数据源 + liteflow + 中信/平安 SM2 密钥占位）。

9. **对象转换约束（Converter）**：参照 lsym consume 的 MapStruct Converter 用法，确立
   “持久层 Entity ↔ VO/BO 转换必须用 MapStruct，禁止手写 set”的硬约束。工程经 `catering-common-core`
   已传递 `mapstruct-plus-spring-boot-starter`，根 pom `annotationProcessorPaths` 全局注册
   `mapstruct-plus-processor`，catering-front 无需在 pom 重复声明。范式：VO/BO 标
   `@AutoMapper(target=Entity.class)` + `MapstructUtils.convert(src, Target.class)` 静态调用，
   参照 `catering-system` 的 `MpConfigDataVo`。约束已写入 `05-front代码开发约束.md` §1.12、§3.9、
   §9、§11。本轮无持久层 Entity，Converter 在下一轮持久层开工时落地。Handle 内 baseData→银行协议
   DTO 的转换不在本约束范围（属银行协议组装）。

10. **依赖版本管理约束**：所有第三方大依赖版本统一在工程根 `pom.xml` 的
    `<properties>` 声明、`<dependencyManagement>` 注册，子模块 `pom.xml` 引用时只写
    groupId + artifactId 禁止写 `<version>`。约束已写入 `05-front代码开发约束.md` §2.5、§9、§11。
    当前 catering-front pom 的第三方依赖（liteflow/lombok/mybatis-plus-generator/velocity/
    spring-boot-starter-test）均未写版本号，走根 pom 管理。

11. **持久层代码生成器**：参照 lsym consume 的 `CodeGenerator`，在 catering-front 的
    `src/test/java/com/chinaums/front/generator/FrontCodeGenerator.java` 建立适配 front 工程范式的
    代码生成器（test scope，非运行时代码）。配套 6 个 Velocity 模板（`src/test/resources/templates/`）：
    `entity.java.vm`（Entity extends TenantEntity）、`vo.java.vm`（VO 标 @AutoMapper）、
    `mapper.java.vm`（Mapper extends BaseMapperPlus）、`service.java.vm`、`serviceImpl.java.vm`
    （Service 用 MapstructUtils.convert）、`mapper.xml.vm`。
    数据库连接信息从环境变量 `FRONT_DB_URL/FRONT_DB_USERNAME/FRONT_DB_PASSWORD` 读取，禁止硬编码。
    明天到公司连上数据库后：配置环境变量 → 改 `TABLE_NAME` → 运行 main → 把生成结果复制到 main 包。

### 14.2 本轮仍未实现（明确 TODO，下一轮）

- 分页明细查询成功分支仍需通过 `TableDataInfo.build(...)` 或显式赋值保证
  `code=200/msg=查询成功`，并从银行 `totalNum` 正确填充 `total`；
- `TenantDataSourceShardingAlgorithm` 仍需删除默认 `ds_0`/第一个数据源兜底：配置缺失、解析失败或
  目标数据源不存在时必须明确失败；
- `FrontErrorCode.IDEMPOTENCY_CONFLICT` 仍需改为“交易已存在”语义：建议使用
  `TRANSACTION_ALREADY_EXISTS(F300001, "交易已存在")`，不得再表达请求处理中、参数冲突或旧结果重放；
- 当前 DTO/Handle 若仍从 `baseData` 读取收付款账户、会员编号或名称，需改为从 `specialData`
  的银行协议原始 key 读取；`baseData` 只保留内部业务系统字段；
- refund 的 ORI_USER_SSN/ORI_USER_TRANS_DT/ORI_USER_D_ID 等需渠道流水的字段，平安退款原交易
  银行字段，均待持久层接入后从原渠道流水加载；
- 中信退款和交易状态查询所需原渠道字段由持久层补齐；
- 平安全部查询接口的字段、bizFunc、额外字段和返回数组结构人工核对；
- 数据库建表执行、配置中心 `zx_bank_config`/`pa_bank_config` 灌数；
- LiteFlow `isAccess` 并发隔离验证、`whenMaxWaitTime` 调优（本轮纯 THEN 串行）。

---

## 15. 下一次建议直接开始的任务

交易首版字段契约已覆盖 transfer/consume、平安授权转账、两家提现/真退款和中信平台收付款。
后续 AI 应一次只实现一个银行和一个方法。
平安授权链路的推荐顺序：

```text
平安 resendTransferAuthCode（bizFunc=26，先获得 smsIdx）
→ 平安 transferAuth（bizFunc=45，使用 messageOrderNo/messageCheckCode）
```

实现授权链路时必须完整阅读 `07-transferAuth-resendTransferAuthCode字段契约.md`，并补齐真实配置 Provider、银行协议 DTO、
显式 reserve 映射、签名加密、HTTP 调用、渠道交易流水和响应归一化。未经用户明确要求，不写测试类，
不运行测试或编译。
