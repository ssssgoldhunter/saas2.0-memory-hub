# 2026-08-30 对话记录

## Front 文档-代码不对齐扫描 + tenant_id 分片校准

- 任务：扫描 catering-front 代码与记忆体，梳理文档设计自身问题与代码-文档不对齐；
  范围收敛为 catering-front 代码 + 记忆体文档（`cateringsass/docs/front/` 快照按用户要求不管）。
- 核心发现：tenant_id 分片改造（`c5cf5ae4`/`7ae51dd6`/`d164c7e7`，2026-08-29 19:07 后合入）晚于
  上次文档校准（`16f0d31`，08-29 17:28），记忆库分片描述整段过时（分片键、路由来源、
  "WHERE 均含分片键"约束、基线提交号全部失效）。
- 本次仅改文档不改代码，改动清单：
  - `05` §3.10.1 重写为 tenant_id 分片 + `TenantDataSourceMappingCache`（映射权威源
    `sys_tenant.resourceConfig`、TTL/single-flight/启动预热、`data_source_id` 仅 insert 列值、
    FR-6 免显式分片键、range 广播口径），三条历史沿革降为引注。
  - `WIKI-START`：基线锚点更新 `master@d164c7e7`；§4 数量口径引用与分片 bullet、§6 失败策略
    约束同步；§7.1 差异表补 `TenantResolveNode` 失效注释与 `FrontFileProcessApi` 无实现两行。
  - `09` §12.1 改写：原文"查 `tenant_base_config` JSON 解析 `data_source_id` 拼 `ds_x`"是
    从未实施的第三种架构描述。
  - `19` 手册"分片键全链路覆盖"改为 tenant_id 口径；`01` §11 分库键改 `tenant_id`。
  - `MEMORY.md`（基线、分片键、静态差异清单）与 `WORK_MANUAL.md` §7.2 同步；
    `12-front-implementation-issues/README.md` 核验行更新（并注明历史 Issue 的
    data_source_id 描述仅作历史记录）。
  - 历史文档（00/04/28/29/30、已关闭 Issue 子文件）按"历史不作当前依据"规则未动。
- 设计层面遗留（未处理，待用户决策）：映射缓存 TTL 无抖动（同批预热租户集体过期，
  single-flight 仅同租户去重，存在跨租户刷新风暴面）；双配置源（`resourceConfig` 管路由、
  `tenant_base_config.data_source_id` 管列值）一致性靠人工流程；工作区根 `docs/` 三份分片文档
  状态口径混乱且 AC-9 写"13 条链"（实际 21 条）。
- §7.1 静态差异复核结论：原六项全部仍存在（去白名单孤儿链、Query/Account null 收口、
  平安 Sender 失败事件、web-test Authorization、双入口日志、注释旧口径），本次新增第 7 项
  FrontFileProcessApi。
- 未运行测试、未执行编译；代码仓库未做任何改动、未 commit。

## 第二轮：按用户反馈逐条回代码复核

- 用户指出文档仍有对不上的地方、代码确实改动了；对已写入文档的每条断言回到代码逐条复核：
  - `eq(DATA_SOURCE_ID, ...)` 显式条件：驼峰/下划线两种风格全量 grep，channel 包零残留——
    "已全部移除"成立（`7ae51dd6` 覆盖 12 交易类 + 2 平安查询类；中信查询类与账户类本就无此条件）；
  - insert 列值：各 Capability 仍 `setDataSourceId` 写列；三个 ExecuteNode 第④步均保留回填+非空校验；
  - 4 参注入：`BaseDataRequestBodyAdvice:89` 仍注入 dataSourceId；
  - 租户插件：front 依赖 `catering-common-tenant`，`tenant.enable: true` 在共享 nacos
    `application-common.yml`，10 张渠道表不在 excludes——插件注入 tenant_id 成立；
  - 缓存实现逐行核对：per-tenant 锁 single-flight、`ds_\d+` 严格校验、SHORT_TTL 5 分钟、
    TTL [10,30] 越界回退 15、预热失败不阻断——此前文档描述全部吻合；
  - dev/uat/prod 三份 yaml 均为 `shardingColumn: tenant_id`，actualDataNodes 10 张表。
- 复核中发现并新增登记的代码事实（均未改代码）：
  - `FrontCiticPlatformPay/PlatformReceiveTransactionMapper` 的 `Base_Column_List` 中
    `data_source_id` 重复出现（`045ab653`"临时提交版本2"引入，早于 cead0222 基线、历次校准未发现）；
  - dev/uat/prod 三份 `shardingsphere-config-*.yaml` 均为 `props.sql-show: true`（含 prod）；
  - `data_source_id` 列值与 `sys_tenant.resourceConfig` 的一致性无代码强校验，05 §3.10.1
    已把"必须同值同格式"修正为数据治理约定口径。
- 模块 `README.md`（185 行）仍准确；`docs/front/` 快照按用户要求未查看未维护。
- 记忆库本轮改动随第二次提交推送；仍未运行测试、未执行编译、未改代码仓库。

## 第三轮：全库一致性扫尾（防多文档前后不一致）

- 全量 grep 记忆库现行文档，清掉 §3.10.1 之外残留的旧分片口径，共 4 处：
  - `05` §9 约束清单：分片键改 `tenant_id`；`FrontDataSourceHelper` 废弃理由改为插件注入路由；
    "分库路由键是 data_source_id"改为"路由键 `tenant_id`、`data_source_id` 仅列值"；
  - `05` 交付自检清单 A（4 参注入）：末条同步 tenant_id 路由口径；
  - `05` 交付自检清单 H（分库与分区）：算法描述改为查 `TenantDataSourceMappingCache`，
    失败策略改为 tenant_id/映射/resourceConfig 三类 fail-fast；
  - `14` web-test 说明 §7.2：`test_context_prepared` 中"front 分库路由读 body 值"改为
    "路由按 tenant_id，body 的 dataSourceId 仅 insert 列值"（header/body 分离理由保留）。
- 一致性复核结论：
  - 06/07/08/10/15/16/17/18 字段契约与 11 公共框架 catalog 零分片/分库表述，无需改动；
  - 20 手册"ExecuteNode 第④步回填、回填后仍缺失则请求失败"与代码一致，保留；
  - ExecuteNode 步骤号①-⑤（Slot→校验→③租户信息→④回填+BankCode→⑤银行配置）与 WIKI/05 的
    第③步/第④步引用一致；
  - `data_source_id` 字样仅存于：列定义语境（09/09A/09B）、"仅作 insert 列值"新口径、
    05 历史沿革引注、§7.1 差异行——均为正确语境；
  - 13 待办、FRONT-ACC-001、FRONT-SEC-001 无分片相关表述；14 的 Authorization 冲突句与
    WIKI §7.1 口径一致（待修复静态差异）。
- 本轮未运行测试、未执行编译、未改代码仓库。

## 第四轮：改错自检（用户要求确认前期修改无误）

- 对三轮改动逐段读回对照代码，发现并修复 3 处自伤/不一致：
  - 05 §3.10.1 重写时丢失旧版"配置位置"条目——已补回并核实：nacos `catering-front.yml`
    以 `ShardingSphereDriver` 加载 classpath yaml，且显式
    `spring.datasource.dynamic.enabled=false` 关闭 common-mybatis 带入的 starter；
  - 跨文档不一致：05 已把"列值与 resourceConfig 同值同格式"改为数据治理口径，但
    09 §12.1 仍写"须同值同格式"——已同步为同一口径（【应当】+代码不校验）；
  - WIKI §7.1 注释行归属含糊——拆清为：TenantResolveNode 仅在 `TenantBankConfigLoader`
    注释；`data_source_id`"分片 SQL"口径在 ExecuteNode 注释。
- 自检通过项（无需改）：租户插件 excludes 全清单仅 11 张 sys 表、不含渠道表——
  "插件注入 tenant_id"结论成立；缓存/算法/yaml/回填/4 参注入此前描述与代码逐条吻合；
  WIKI §4/§6/§7.1 读回连贯；"同值同格式"全库仅 05/09 两处且口径一致。
- 本轮未运行测试、未执行编译、未改代码仓库。

## 第五轮：同步 cateringsass/docs/front/ 副本（用户要求）

- `cateringsass/docs/front/` 共 7 份副本（01/05/14/19/20/21/27），其中 4 份因记忆库
  tenant_id 校准产生差异，已用记忆库最新版（`d2cd2cd`）覆盖，7 份全部恢复逐字节一致；
- 20/21/27 记忆库侧本就未改，无需覆盖；
- cateringsass 工作区 4 个文件已改、**未 commit**——按 WORK_MANUAL 规则等用户确认；
- 口径建议（未执行）：该目录没有 WIKI-START/09/12README，团队成员从代码仓库读文档时
  看不到入口页与分库 DDL 规则，是否扩集由用户决定。
