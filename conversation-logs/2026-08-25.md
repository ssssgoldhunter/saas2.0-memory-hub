# 2026-08-25 对话记录

## Catering Front 扁平化设计重写

- 用户决定放弃当前未提交的全面改造代码，只优化设计和工程文档。
- 最终结构裁决：Slot 严格两层；`flow` 按 `slot/node/route` 分包；银行模块按能力分包，真复用放银行
  `common`；Capability 按校验、组报文、查重/三阶段流水、统一发送、响应落库/结果顺序展开。
- 明确禁止：业务 Context、多层 Handle 继承、Router/Dispatch、BankSupport God class、多层钱包 Client、
  为减少少量重复拆分业务流程、脱离 `com.chinaums.front` 的 package 声明。
- 钱包业务发送只允许 `BankWalletGateway.post` 一个出口，Gateway 只再分派到直接执行 HTTP 的银行 Sender；
  日志执行用户确认的 B 方案：Capability 记录真实业务步骤，最终 Sender 统一且只记录一次钱包请求、
  响应或失败；删除旧 Handle/Capability 的重复钱包报文日志，其余字段范围和安全边界沿用代码基线。
- 租户配置加载保留清晰链路但彻底压缩层级：
  `TenantResolveNode → TenantBankConfigLoader → RemoteConfigServiceClient`；Loader 是具体类，直接完成查询、
  校验与扁平组装，不保留 `Provider → AssemblerRouter → Assembler` 及抽象/继承体系。
- Loader 只保留 `loadTenantBaseInfo` 与 `loadBankAccountConfig` 两个公共方法；中信不明来款 Application
  Service 将旧 Provider 依赖替换为同一 Loader，专项 API、Channel 与业务行为不变。
- 文件数和类行数不作为目标，验收标准是打开一个 Capability 即可读完整主流程。
- 用户最终将实施范围改为全量迁移：中信 11 个、平安 11 个通用能力与 13 条 LiteFlow 链全部切换；
  平安账户状态/余额保留挡板，平台收付款继续不支持。
- 文档更新：28 号目标设计、05 开发约束、WIKI 入口、19 框架手册、16 号已放弃尝试说明。
- 本次只修改文档，未清理代码、未编译、未运行测试；用户要求暂不提交或推送，等待其回来 review。

## 用户原始需求复核

- 用户再次明确：Slot 参考 lsym UAT `catering-consume`，固定为 `FrontBaseSlot` 及直接继承它的
  `FrontTransSlot`、`FrontQuerySlot`，不得使用业务 Context 命名或增加第三层。
- Front 框架职责保持 Registry + Route；租户/配置加载直接完成，不为一次加载建立继承链。
- `catering-api-front`、调用方组装和 API 接口不动。
- 按“银行 × 能力”扁平放置，允许各能力保留自己的组装代码和少量冗余，优先保证顺序可读。
- 日志参考旧 Handle 的就地方式；钱包只有一个统一业务发送出口，不增加多层调用包装。
- 已据此修正 28/29、05、19 和 WIKI：固定代码基线 SHA、补齐 `BankCapability` 签名、明确最终 Sender
  层级、B 方案日志归属和简化后的租户配置链。
- 用户明确本轮不新增/运行测试、不执行编译，只做静态 review。
- 用户进一步确认最终框架验收口径：扁平化后仍保留 LiteFlow + 注册式 Route；API 入口不变；处理按
  “银行 × 能力”拆分，能力内部自行扁平组装；钱包通过统一接口发送；Capability 记录业务步骤与异常，
  最终 Sender 记录发送前、响应后和通信异常。未来不新增能力、只接入新银行时，应只增加银行配置分支、
  Sender 与对应 Capability，不修改 API、LiteFlow、Registry 或 Route。
