# 2026-08-17 对话记录

## Oh-DSH Desktop：modlens 视觉变体回归修复

### 问题现象

模型选择器切到「DeepSeek-V4-Flash (modlens vision)」时报：
`DeepSeek (modlens vision) 加载失败：model "deepseek-v4-flash" is outside the modlens vision wrap scope`

### 根因

1. `@oh-dsh/desktop` bundle（`~/.ohdsh/profiles/node_modules/@oh-dsh/desktop/dist/cordis.patch.yml`）
   通过 `insert` 注册 `oh-vision`（`@oh-dsh/vision` v0.1.5）。
2. `oh-vision` 的 `installDeepSeekV4ImageAdmission`（`~/.ohdsh/profiles/node_modules/@oh-dsh/vision/dist/index.js:445`）
   包装 `llm.resolveModelInfo`，对**所有 `deepseek-v4*` 模型强行注入**
   `inputModalities: [..., "image"]`（即使模型实际是 text-only）。
3. modlens 的 `resolveModel → shouldWrap()` 看到 `inputModalities.includes('image')`，
   判定"已是视觉模型，无需包装" → 抛 `outside the modlens vision wrap scope`
   （`~/.ohdsh/profiles/desktop/node_modules/@liustack/modlens/dsh/index.js:556`）。
4. 记忆体 ENVIRONMENT.md §1.4 记录的修复 = `cordis.patch.yml` 必须含
   `- id: oh-vision / disabled: true`，但该条目**已缺失**（仅剩 `dsh-obsidian-assistant`）→ 回归。

### 修复

- `~/.ohdsh/profiles/desktop/cordis.patch.yml` 恢复：

```yaml
- id: oh-vision
  disabled: true
```

- 附注释说明"必须保留"原因（对应 ENVIRONMENT.md §1.4）。
- 生效方式：patch 在 Desktop **启动时**应用，需重启 Oh-DSH Desktop。
- 副作用：`view_image` 工具消失（符合记忆体设计：视觉由 modlens 提供）。

### 待办 / 后续注意

- 重启后验证「DeepSeek-V4-Flash (modlens vision)」可正常加载。
- 日志仍见 `[modlens] paste-to-path route skipped: webserver: duplicate exact route`，
  疑似 modlens 在 dependencies 与 bundles 双重加载，不影响功能，可后续清理。
- 该 `oh-vision: disabled` 条目易丢失（本次即回归），建议 ENVIRONMENT.md §1.4 增加
  "缺失时症状" 说明，便于快速定位。

## 相关文件

- 修改：`/Users/limeng/.ohdsh/profiles/desktop/cordis.patch.yml`（工作区外，desktop profile）
- 记忆体依据：`saas2.0-memory-hub/agent-environment/ENVIRONMENT.md` §1.4

---

## 环境配置补全：DSH.md 全局指令部署 + 默认 agent 预设

用户要求：把 DSH 工作手册拿出来用，并按环境配置创建 agent 模型。

### 现状核对（对照 ENVIRONMENT.md）

| 配置项 | 要求 | 修改前 | 处理后 |
|--------|------|--------|--------|
| 全局指令 | `$DSH_HOME/AGENTS.md` = DSH.md | 缺失（`~/.ohdsh/AGENTS.md` 不存在，仅 web 端 `~/.dsh/AGENTS.md` 有） | ✅ 已部署（IDENTICAL） |
| agent 预设 | `~/.ohdsh/.agent-presets/ssss-dsh/` | ✅ 已存在且与记忆体 IDENTICAL | 无需改 |
| 默认预设 | settings `agent-presets.default = ssss-dsh` | 缺失（系统默认 `standard`） | ✅ 已写入 settings.yaml |
| 默认模型 | deepseek-v4-flash / deepseek-official / high | ✅ 已有 `agent-default-model` | 无需改 |

### 关键机制（源码确认）

- 全局指令加载：`@deepseek-ai/dsh-agent-instructions` 读 `$DSH_HOME/AGENTS.md`（`files.js` `userGlobal = join(config.dshHome, 'AGENTS.md')`），Desktop 的 DSH_HOME=`~/.ohdsh`，故此前 Desktop 会话**未加载** DSH.md。
- 预设默认：`@deepseek-ai/dsh-agent-presets` 的 settings namespace = `agent-presets`（kebab-case），schema `{ default: string }`；用户层 settings 覆盖系统默认 `standard`（web-app patch 注册 `config.default: standard`）。

### 变更

- 部署 `~/.ohdsh/AGENTS.md` = 记忆体 `agent-environment/DSH.md`（cp + diff 验证 IDENTICAL）。
- `~/.ohdsh/settings.yaml` 新增：

```yaml
agent-presets:
  default: ssss-dsh
```

- 记忆体 ENVIRONMENT.md：§0 全局指令行更新为桌面端路径；§6 补充"缺失时回退 standard"提示。

### 生效

- AGENTS.md：新会话生效（当前会话已通过 system-reminder 注入确认）。
- 预设默认：settings 热加载，新会话生效（运行中会话保持原预设）。
- 验证：重启 Desktop 后新会话应自动使用 ssss-dsh 预设 + deepseek-v4-flash。

---

## specialData 标准化实施（15/16 号 spec/plan 第三次架构修正并落地）

### 架构定稿（用户裁决）

- 组装能力以 **catering-api-front 实例工具类**提供（`FrontSpecialDataAssembler`），本地调用，
  不服务化、不 Feign，**front 服务零改动**；此前的 FrontAssembleApi/Controller/Registry/注册器方案作废；
- 工具类**全实例方法零 static**（用户要求）：大对象 + 嵌套小结构（AccountInfo/BankCard/Auth）+
  `newPay()/newRec()/newOriPay()/newOriRec()/newAuth()/newBankCard()` 工厂；银行路由写死
  （switch BankCode → 银行私有内部类 → switch capability，12 个能力映射：中信 6 + 平安 6 含平安退款空实现；
  原 spec"13 个绑定"系重复计数笔误，已修正为 12）；
- consume 侧 check **按能力维度**（不按银行×能力）：7 个（consume 树 5 + fund 树 2），
  平安退款无需特判（工具类统一返回空对象）；DeductionTrans 扣款走 TRANSFER 能力（用户确认）；
  PlatformPay/Receive 第一期不做 check（consume 无调用链）。

### 代码落地（cateringsass 分支 limeng_front，已提交推送：d5dd39ca / 32d4b380 / 96fb145b，含用户 application.yml self-fund-type 调整；catering-modules/pom.xml 的 reactor 裁剪为用户本地改动未提交）

- 新增 `catering-api-front/.../api/assemble/FrontSpecialDataAssembler.java`：
  **已编译通过并 install 至 /tmp/mdl-m2**；
- 新增 `catering-consume/.../flow/component/base/specialdata/`：基类 + 7 个 check 骨架
  （buildRequest TODO 占位注明计划数据源，调用/校验/回填/失败终止为实装）；
  基类取 slot 用 `this.getFirstContextBean()`（对齐现有组件；LiteFlow 2.12.1 无 getSlotBean(Class)，
  定点 javac 发现后已修复）；
- 两个 `TransSlot`（consume 树 + fund 树）各加 `assembledSpecialData` 字段；
- **check 未挂链**：老树银行组件 stub 待适配、fund 树在线走旧 FacadeApi，挂接随各链 front 适配进行；
- 用户指令：**不编译、不跑测试**，只新增自己部分；catering-modules/pom.xml 临时启用 consume 构建
  后已还原为用户本地状态。

### 存量问题（非本次引入，待另行修复；用户次日安排人纠正 consume 双 slot）

1. consume 全模块编译不可用：`com.chinaums.report.{api,request,response}.catering` 包缺失、
   `BaseMerchantFacadeApi` 缺失、`BasTransWithDrawRes.java` 被置空（fund TransSlot 引用报错）；
2. consume/fund 两棵树两个同名 TransSlot 为存量结构问题；
3. web-test application.yml 未提交改动（租户 self-fund-type 015001→001002）与 MEMORY.md §6 记录不一致，待确认。

### 待办

- web-test 两步调用（16 号 Phase 3）✅ 已于同日完成（见下节）；buildRequest 补实与挂链（Phase 4，
  依赖账户体系 storeNo 定型）；文档联动 05/06/07/08/13（Phase 5）✅ 已于同日完成。

## web-test 两步调用改造（16 号 Phase 3，同日完成）

- `FrontTestController` 新增 `POST /api/test/front/assemble/special-data`（本地调组装工具类，
  FrontException→R.fail，日志脱敏只记键名）；
- `app.js`：交易 Tab 协议键 schema 全部替换为标准结构（std-*：pay/rec/oriPay/oriRec/bankCard/auth +
  originalBusinessDate/contractId）；execTab 两步（组装→确认弹窗展示组装结果→发交易）；
  resendAuth 两步无弹窗；门店信息直接取账户填 baseData；dealType/fundTp 不再上送（红线）；
  查询 Tab 不变；node --check 语法通过，模块编译/冒烟按用户指令未执行；
- 14 号使用说明 §4/§6.2、16 号 plan Phase 3、05/06/07/08/13 号文档联动全部同步（见当日提交）。

## 组装工具类工厂化重构（同日晚，用户裁决）

- 用户要求：一个银行一个独立组装功能，`FrontSpecialDataAssembler` 按 platformCode + capability
  调用不同组装接口，类似工厂类格式；
- `com.chinaums.front.api.assemble` 从单文件拆为 4 文件：`BankSpecialDataAssembler`（接口）+
  `CiticSpecialDataAssembler` / `PingAnSpecialDataAssembler`（同包 package-private 独立银行组装类，
  构造持有当次数据实例、非 Spring Bean）+ 主类（数据 + `bankAssembler()` 工厂：switch BankCode
  创建银行类，银行类内 switch capability，两级寻址）；
- 校验辅助（require/requireDate/capabilityNotSupported）降为包内可见供银行类复用；
  新增银行 = 新增组装类 + 工厂加 case；15 号 §2/§3.2/§5、05 号 §2.2 表述同步；
  调用方（web-test/consume check）零改动；按用户指令未编译，待存量修复后随模块编译验证。

## 标准结构增加证件要素预留（同日晚，用户裁决）

- 核实结论（先答用户问题"平安提现需要证件号吗"）：**不需要**——提现模式一 bizFunc=01 为
  "会员提现-不验证 6033"（03 号）；08 号 §3.2 将 certNo 列为登记字段（映射 reserve.certNoEnc）
  但 Handle 必填校验仅 5 键，certNo 有值才加密上送；
- 用户决策：AccountInfo 增加 `certNo` + `certType` **通用预留字段**（平安场景可能需要，
  目前不组装上送；certType 平安提现协议固定 24 仍由 Handle 处理）；激活时在对应银行组装器加
  "有则输出"并补矩阵行；web-test 暂不加输入框（避免填了无效的困惑）；15 号 §3.1/§3.2 已同步。

## master 合并后复查（同日晚）

- 合并方式：origin/master → limeng_front（39e468e3，本地领先 master 9 提交）。
- catering-front：合并为**纯加法**——划付文件处理（FileProcess* 服务/路由/ZxFileProcessHandle 46 文件
  +3177 行；SaasZxInterService 整体注释为未完成迁移体）；交易链、ContractKeys、Handle、front-flow
  零改动，组装工具设计前提不变。
- consume：划付迁移删除整个 fund 包（255 文件 +3064/-17253），fund slot 全删（用户暂存删除最后一个
  fund TransSlot.java，slot 统一）；chainWithDraw 清空、chainDeduction 仍引用已删 bean（启动雷）；
  chainConsume 的 accountCheck 等被注释；BaseMerchantFacadeApi 仍缺失；BasTransWithDrawRes 仍为
  空文件（已无引用者，建议删文件）。
- check 复查处置：5 个 consume 树 check 不受影响（slot 与字段均存活）；withdraw/deduction 两个
  check 的 fund import 悬空，已重指向 consume TransSlot 并更新注释（已改未提交，与用户暂存的
  slot 删除一起待提交）。
