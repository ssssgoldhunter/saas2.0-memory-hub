# 2026-08-24 对话记录

## 中信不明来款专项能力开发、复核与交付

- 目标：在 SaaS 2.0 `catering-front` 提供中信不明来款完整能力；先完成方案与协议对齐，再由其他 AI
  开发并经过多轮 review，最终由 Codex 修复剩余问题、更新记忆体和接入文档并提交。
- 最终协议基线：`中信E管家产品V2_不明来账_客户钱包应用平台_接口文档-内部集成平台.doc`；
  lsym UAT 用于核对实际调用形态。固定采用 `2033/2025/2023/2087 + chnlNo=0010`，不采用综合文档
  的 `24/123` 体系。
- 架构裁决：能力仅属于中信，独立 `CiticUnidentifiedRemittanceApi`、Controller、Application Service、
  Channel、协议 DTO 和 Mapper；不进入通用 `FrontCapability`/Router/Registry/LiteFlow；请求与返回均为
  全字段强类型 DTO，不使用 `specialData`。租户数据继续复用请求上下文注入、`tenant_base_config` 和
  `TenantBankConfigProvider`。
- 对外入口：
  1. `/front/v1/citic/unidentified-remittances/pages`：2033 列表查询；
  2. `/front/v1/citic/unidentified-remittances/process`：2025 退款、2023 重新匹配/实时清分；
  3. `/front/v1/citic/unidentified-remittances/status`：2087 状态查询。
- 最终复核修正：重新匹配不再误要求 `userId`；2023 客户账的
  `acctSeq/acctTransNo/finTransFlag` 与 `bankNo` 一起进入银行 `reserve`；列表 `transStatus` 只允许
  `0/1/9`；2087 明确使用列表 `transJrno/transDate`，不使用处理请求 `frontSsn`。
- 文档更新：`MEMORY.md`、`WIKI-START.md`、02 号中信能力汇总、19 号框架设计手册；新增
  `27-中信不明来款业务接入手册.md`，并同步 19/27 到代码仓 `docs`；专项 Word 原文保存在两个仓库
  对应 `docs` 目录。
- 验证：`mvn -pl catering-modules/catering-front -am -DskipTests=false -Dcheckstyle.skip=false test`
  通过，Reactor `BUILD SUCCESS`，Checkstyle 通过，34 个专项测试全部通过（失败 0、错误 0）。
- 代码仓提交：`99e696f4 feat(front): 支持中信不明来款专项能力`；记忆库文档由本次提交保存。
