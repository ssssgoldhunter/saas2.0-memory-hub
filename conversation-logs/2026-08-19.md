# 2026-08-19 对话记录

## 开发对接手册整理

- 任务代号：开发对接手册整理。
- 目标：为后续开发人员与 AI 提供 `catering-front` 开发支持，并为业务上游提供交易、交易查询的实际对接依据。
- 以当前 `catering-api-front` DTO、Feign 接口、Assembler、LiteFlow、Registry、Router、银行 Handle、渠道表和 `catering-consume` 代码为事实源，新增三份正式手册：
  1. `19-catering-front框架与业务功能设计手册.md`；
  2. `20-catering-front交易接口对接手册.md`；
  3. `21-catering-front交易查询接口对接手册.md`。
- 删除陈旧且不完整的 `13-front-api-external.md`，在 `WIKI-START.md` 增加三份正式手册入口，并修正 15 号 spec 的有效引用。
- 手册只登记当前真实状态：中信 8 个交易入口中实现 6 个、5 个查询均实现；平安实现 6 个交易能力及交易状态/两类明细查询，账户状态与余额仍为 `ADAPTER_NOT_READY`。
- 明确 `catering-consume` 仅有 specialData check 骨架，`buildRequest()` 和正式调用链尚未接通，不把骨架统计为完成能力。
- 24 查询当前源码不默认 `accountType=12`；仅中信使用，允许 `01/12/13/17`，未传时不输出 `registerAttr`。该结论按工作区当前源码记录。
- 静态验收：8 个 `FrontTransApi` 方法、5 个 `FrontQueryApi` 方法、公共及专有 DTO 字段均在手册中逐项命中；Markdown 代码块成对，Wiki 链接存在，旧 13 文件已删除。
- 本次仅整理文档，未修改业务代码，未运行测试或编译，未提交或推送。
