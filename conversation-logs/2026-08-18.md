# 2026-08-18 对话记录

（承接 2026-08-17 深夜会话的收尾，日期跨零点）

## master 合并后复查 + 交易状态查询全链路落地

### master 合并复查

- 合并方式：origin/master → limeng_front（39e468e3）。front 侧为纯加法（划付文件处理 46 文件
  +3177 行，SaasZxInterService 为注释体），交易链/ContractKeys/Handle 零改动。
- consume 侧划付迁移删除整个 fund 包（-17253 行），slot 统一为 consume 树；用户暂存删除最后的
  fund TransSlot；withdraw/deduction 两个 check 已重指向 consume slot（未提交）。
- 存量雷上报：chainDeduction 引用已删 bean、chainConsumeAuth 引用缺 BaseMerchantFacadeApi 的
  check、BasTransWithDrawRes 空文件（已无引用者，建议删文件）。

### 交易状态查询（按用户逐条裁决落地，全部未提交）

1. **组装**：TRANSACTION_STATUS_QUERY 进组装工具（中信 pay.bankEAccountId→acctNo；
   平安 pay.bankEMemberCode→mchntMbrId，各 1 要素）。
2. **平安实现**：去掉 pendingIntegration；bizFunc 三模式（WITHDRAW→03 / RECHARGE→04 /
   会员间→02，lsym 生产规则）；frontSsn 平安必填（oriTransSsn 定位）。
3. **状态映射**：新增 FrontInternalTransStatus（S/P/F 常量）；TransactionStatusResult.frontStatus
   改 String 三态，未知/空→null；银行状态码在各 Handle 以带注释常量维护
   （中信 00~05：04/05→S；平安 0/1/2/5/6）。
4. **RECHARGE 进 FrontCapability**（不带 org 前缀）：仅平安 04 使用，中信 default 拒绝。
5. **银行请求合并 → 五点修正定稿**：QueryTransStatusRequest 银行无关，只含
   capability/transactionDate/bizOrderNo/bizSubOrderNo/frontSsn + specialData（组装结果），
  `QueryTransStatusRequest(context)` 实例构造（无 static）；信封全部收回 Handle
  （常量+账户配置+序列生成，wire JSONObject 键走 FrontBankRequestConstants，补 3 常量）；
   CiticQueryTransStatusRequest 删除，PingAnQueryRequest 加 TODO[QUERY-UNIFY] 过渡标记
   （4 个查询能力迁完统一模式后删除——查询统一路线）。
6. **平安提现卡号特殊操作**：03 查询专用 cardNoEnc 从平安提现渠道表按
   (tenantId, frontSsn) 回查 bankCardNo 后 SM2 上送，不经调用方。
7. **对外契约去 original 前缀**：TransactionStatusQueryData（capability/transactionDate）+
   FrontQueryApi + 两 Handle 报错 + web-test + 05/10/13 号。
8. 修复 buildTransStatusWire 泛型通配符编译错误（context 参数改具体类型）。
9. `bankEAccountId→cardNoEnc` 疑问结案（用户）：系 lsym 99 验证码路径，交易状态查询用不到。

### 文档同步（多次推送，最终 7839512）

13 号 §5.3 重写为最终形态（平安表/frontSsn 必填/RECHARGE/响应 P-F-S/组装提示）、§2.0 查询组装；
10 号（三件套+交易类型区分+统一请求五点修正+查询统一路线）；14 号查询 Tab 口径；
15 号 §4.3 查询组装格；16 号实施记录与收盘状态。

### 遗留（下次会话）

- 代码已提交推送 origin/limeng_front（b41d885e consume slot 配套 / da4e833c api+common 支撑 /
  3260241a front 实现 / a58c33d8 web-test，用户指令 2026-08-18；**仍未编译**，团队拉取需注意）；
- 中信退款状态查询 TRANS_TYPE=01 为推断值，待协议核对；
- 账户状态/余额/平台明细/账户明细 4 个查询能力迁移统一模式（含 PingAnQueryRequest 删除）；
- consume 存量修复（用户安排人）+ check buildRequest 补实与挂链；
- web-test 启动冒烟（交易两步 + 交易状态查询两步）。
