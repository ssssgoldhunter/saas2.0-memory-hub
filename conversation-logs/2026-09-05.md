# 2026-09-05 对话记录

## catering-front 日志补齐（按用户四项裁决）+ 文档追平当前 HEAD

- 背景：08-30 会话后，其他会话合入了大量改动（frontTenantPack 两节点链、depositReg 入账登记、
  TI/单张凭证下载、中信文件 8 端点、front-flow 头注释修复、TenantResolveNode 注释清理等），
  本地 HEAD 为 `limeng_front@66d7df9d`；记忆库曾按中间快照 `dbd9fad5`/`e3e21604` 校准，
  数量口径（29 实现类/21 链/枚举 21/标准 API 20）已落后于 HEAD。
- 用户裁决（AskUserQuestion 四项，均按推荐项执行）：
  1. 追踪号 = traceId + frontSsn 双轨：`FrontTraceFilter`（已在 HEAD，队友实现）读请求头
     `reqId` 写 MDC、缺省生成 `ex_` 前缀；frontSsn 进响应 payload 与状态查询 metadata。
  2. 校验类 FrontException 日志 = Flow 节点抛出点集中补。
  3. CITIC 两个入口（文件 5→8 端点、不明来款 3）= 扩切面覆盖。
  4. 范围 = 全量 20+5+3（现口径 21 标准 + 11 专项）。
- 代码改动（仅日志，未提交，等用户确认；未编译未测试）：
  - 新增 `logging/FrontLogMetadata.java`：Slot → 定位字段（tenantId/capability/platformCode/
    dataSourceId/storeId/bizOrderNo/bizSubOrderNo/frontSsn）；
  - `FrontInvocationLogAspect`：切点 `controller..*Controller` 覆盖 citic 子包；新增
    `FrontBaseRequestData`/`BaseRequest` 直参分支；状态查询带 `frontSsn`；
  - 四个 Flow 节点 `process→doProcess` 包装：`FrontException` 中断时输出 `flow_interrupted`
    （含 Slot 定位字段 + traceId）后原样抛出；Loader 异常随节点包装覆盖；
  - `logback-plus.xml` 模式补 `[%X{REQ_ID}]`，纯文本日志（统一异常处理器等）也带追踪号。
- 文档追平（本轮记忆库改动）：WIKI-START §2 基线/§4 数量（枚举 23、实现类 30=12/6/12、
  链 22=8/3/11、标准 API 21、入口链路日志 bullet、§7 实况、§7.1 注释行、§8 账户维护
  补 depositReg）；MEMORY §5.3（基线/数量/架构两节点/日志补齐，修复断句）；05 §11 快照与
  四节点检查项；19 手册基线与数量。
- `cateringsass/docs/front/` 7 份副本已全部同步到当前记忆库（05/14/19/20/21/27 本轮有差异，
  01 一致）。
- 遗留：cateringsass 工作区有日志改动 + 副本同步两类未提交改动，按规则等用户确认后提交；
  §7.1 中平安 Sender 失败事件、Query/Account null 收口、web-test Authorization、双入口日志、
  Mapper 重复列、sql-show=true 等差异仍未处理。
