# 2026-08-29 对话记录

## Catering Front 源码反向校准项目文档

- 用户要求以当前代码为事实源更新记忆体文档，不修改代码。
- 静态核验基线：`cateringsass` 分支 `limeng_front`，提交 `cead0222`，代码工作区干净。
- 当前数量：8 个交易 API、5 个查询 API、7 个账户维护 API、3 个中信不明来款专项 API；
  `FrontCapability` 枚举 21 项；银行 Capability 实现类 29 个（12 / 6 / 11）；LiteFlow 链 21 条
  （8 / 3 / 10）。
- 文档统一采用三域扁平调用链、两层 Slot、`routeCapability`、Capability 内可读持久化主流程、
  Gateway/Sender 统一钱包出口；22 个实现类 / 13 条链只保留为 28/29 号历史迁移基线。
- 日志裁决统一为：业务请求/响应 payload（含业务验证码）允许明文；`appKey`、私钥、签名材料、
  签名/认证 Header、Authorization、Cookie 和完整银行 URL 等非业务凭证仍禁止输出。
- 当前源码静态差异已登记但未改代码：`chainFrontAccountUnwhiteName` 无调用方；Query 单条/分页和
  Account 查询仍可能产生 null 成功/空返回；平安 Sender 缺结构化 `wallet_request_failed`；
  web-test 会记录 Authorization Header；Controller Aspect 与 Application Service 都记录完整请求；
  部分 XML/Java 注释仍使用历史 13 条链、Handle/旧 Registry 口径。
- 更新范围包括 MEMORY、WORK_MANUAL、开发操作手册、WIKI、01/04/05/08/13/14/16/19、
  FRONT-ACC-001 和 Issue README。保留并叠加用户已有的未提交文档修改。
- 本次未运行测试、未执行编译、未 commit、未 push。
